use crate::slip::Slip;

fn get_slip(blockID: i64, txID: i64, slipID: i32) -> Slip {
    Slip {}
}
fn add_slip(blockID: i64, txID: i64, slipID: i32, slip: Slip) {
    
}
fn remove_slip(blockID: i64, txID: i64, slipID: i32) {
    
}

#[cfg(test)]
mod test {
    #[test]
    fn test_get_slip() {
        assert!(false);
    }
    #[test]
    fn test_add_slip() {
        assert!(false);
    }
    #[test]
    fn test_serialize() {
        assert!(false);
    }
    #[test]
    fn test_validate() {
        assert!(false);
    }
}