use crate::{
    block::Block,
    types::SaitoMessage,
};
use std::sync::{Arc, RwLock};


pub struct Network {
}

impl Networ {

    /// Creates new `Network`
    pub fn new() -> Self {
         Network {
         }
     }
}



